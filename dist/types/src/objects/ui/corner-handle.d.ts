import { resizeHandlePosition } from '@ui/interactions/use-resize';
type Props = {
    position: resizeHandlePosition;
    inset?: boolean;
};
export declare function CornerHandle({ position, inset }: Props): import("react/jsx-runtime").JSX.Element;
export {};
